using System.Collections;
using System.Collections.Generic;
using UnityEngine;



public class StageGM : MonoBehaviour
{

    public bool item;
    // Start is called before the first frame update
    void Start()
    {
        item = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
